import React from 'react';
import classes from "../../styles/Page.module.css";

const ManufacturerLotsList: React.FC = (): JSX.Element => {
    return (
        <div className={classes.centeredMessage}>
            ManufacturerLotsList
        </div>
    );
};

export default ManufacturerLotsList;